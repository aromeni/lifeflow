# Stage 11A Phase 4D — Emergency-Stop Plan

**Status:** Defined, not yet exercised live (no trigger has occurred; the live checkpoint has not begun) · **Date:** 2026-08-04

Every trigger below shares the same **immediate action** template unless noted otherwise: (1) the owner stops interacting with the browser/consent screen immediately, without completing the pending step; (2) if a callback already returned control to LifeFlow, no further request is made; (3) proceed directly to the standard cleanup sequence — [revocation-results.md](revocation-results.md) Step A (disable initiation) → Step B (revoke) → Step C (disconnect) → Step D (residue check), described fully in [disconnect-and-residue-results.md](disconnect-and-residue-results.md); (4) record the trigger and its classification in [defect-register.md](defect-register.md); (5) the phase decision cannot be PASS while any trigger's root cause is unresolved.

| Trigger | Provider actions that must stop | Evidence to retain | Evidence prohibited | Revocation path | Disconnect path | Severity | Restart criteria |
|---|---|---|---|---|---|---|---|
| Wrong Google account selected | All — do not click consent | Trigger name, timestamp | Account address, screenshot | N/A (no token issued) | N/A | P0 | New owner authorisation after confirming private-browser hygiene |
| Personal/business account visible as selected | All | Trigger name only | Any identifying detail | N/A | N/A | P0 | Same as above |
| Unexpected scope displayed | All — cancel consent | Which scope category (Gmail/Calendar/other) was unexpected, content-free | The literal scope string if it names real data | N/A | N/A | P0 | Fix scope configuration; rebuild acceptance row S11A-P4D pertaining to scopes; re-verify `scope-verification.md` before retry |
| Unexpected consent-screen application name | All — cancel consent | That a mismatch occurred | Screenshot | N/A | N/A | P0 | Fix OAuth app configuration (Phase 4C environment) |
| Incorrect redirect URI | All — cancel before completing | Content-free confirmation of mismatch | The actual URI if it differs from the two approved values | N/A | N/A | P0 | Correct client configuration; re-verify presence-only readiness |
| Callback replay | Stop the second callback attempt | `invalid_state` was returned (already proven safe by regression test) | N/A | Proceed via Step B if a credential exists | Proceed via Step C | P1 if it ever produced a different result than the tested one; otherwise not a live trigger | Investigate before any further live attempt |
| Multiple token exchanges | Stop before a second `/token` POST | Call-budget guard's refusal (content-free count) | N/A | Step B | Step C | P0 | Investigate why the guard's 1-call budget was reached twice; do not retry without a fix |
| Missing PKCE verification | Stop before completing exchange | That PKCE was absent, content-free | N/A | Step B if a token exists | Step C | P0 | Code fix + regression test before any further live attempt |
| Owner/account binding mismatch | All | Which check failed (content-free) | Account identifiers | Step B | Step C | P0 | Code fix + regression test |
| Credential stored as v1 | All further reads | Gate's non-zero count (content-free) | N/A | Step B | Step C | P0 | Credential-gate/key-ring investigation before any retry |
| Unknown key ID | All further reads | Gate's non-zero count | N/A | Step B | Step C | P0 | Same as above |
| Plaintext token exposure (DB column, log, Redis, browser storage, URL, metric, Audit History) | All | That the sentinel scan found an exposure, content-free location only | The token value itself, under any circumstance | Step B | Step C | P0 | Fix the leak, add a regression sentinel test, full independent re-audit before any further live attempt |
| Automatic provider import / background sync / `SourceItem` creation | All | Which mechanism fired (content-free) | Any imported content | Step B | Step C | P0 | Fix the import boundary (see `import-and-background-block-results.md`); add regression coverage |
| Gmail or Calendar write request | Stop the request before or immediately after transmission | The live transport guard's refusal, or — if it somehow reached Google — the exact operation attempted (content-free) and its provider response status only | Any written content | Step B | Step C | **P0 — the single most severe class this phase defines** | Full incident review; manual verification via the owner's own Google account that nothing was created; code fix; independent re-audit |
| Call-budget exceedance | Stop the exceeding request | The guard's refusal | N/A | Step B if applicable | Step C | P0 | Investigate the extra call; fix; re-verify budget tests |
| Deliberate or automatic token refresh | Stop before the refresh request | That a refresh was attempted (content-free) | N/A | Step B | Step C | P0 | This phase's tooling has no refresh code path by design — a P0 here means that design was violated; fix before any retry |
| Inability to disable OAuth initiation | Treat as if initiation is still enabled — full stop | The failure mode (content-free) | N/A | Step B if a token exists | Step C | P0 | Fix the configuration mechanism; verify with `preconnection_readiness_check.py` before any further action |
| Inability to revoke (Step B genuinely fails, not merely uncertain) | N/A (revoke already attempted) | The failure, content-free | N/A | Ask the owner to revoke manually via Google Account security settings | Step C regardless | P1 | Owner manually confirms revocation before closing the phase |
| Inability to disconnect | All | The failure, content-free | N/A | Already attempted | Investigate and fix the disconnect route/service before any further owner action | P0 | Fix before any further live interaction of any kind |
| Residual credential material after cleanup | N/A | The gate's non-zero count | N/A | Re-attempt Step B if a credential still exists | Re-attempt Step C | P0 | Full residue investigation; independent re-audit; phase cannot be PASS |
| Real, client, or participant information encountered in any response | Stop reading further content immediately | That prohibited content was encountered, content-free — never the content itself | The content itself, under any circumstance | Step B | Step C | P0 | This should be structurally impossible (Account A/B are disposable and synthetic) — treat as a P0 investigation into how real information reached a disposable test account |
| Unresolved P0 or P1 from any of the above | N/A | Per the specific trigger above | Per the specific trigger above | Per the specific trigger above | Per the specific trigger above | Inherits the underlying trigger's severity | Phase decision is **FAIL — REAL PROVIDER CONNECTION REMAINS BLOCKED** until resolved |

## Status as of this evidence document

No trigger has occurred — the live checkpoint has not begun. This document defines the response plan in advance, per the governing instruction's requirement that emergency-stop conditions be observed *during* the live sequence, not designed after the fact.
