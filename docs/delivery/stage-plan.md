# Delivery Stage Plan

**Status:** Stage 11 (evaluation and pilot readiness) is next and has not begun; Stage 10 (product design system and UX completion) is complete, merged to `main`, and tagged `stage-10-complete` · **Date:** 2026-07-30

**Roadmap note (2026-07-30):** the original plan (see the table below) numbered "Evaluation & pilot readiness" as Stage 10 and "Packaging & commercial base" as Stage 11. A previously-unplanned product-design stage was inserted, implemented, merged, and tagged as **Stage 10 — Product Design and UX Completion** (`stage-10-complete`). To keep Git tags and completed history immutable, every still-unstarted original future stage has been shifted forward by one number rather than renumbering anything completed: **Evaluation & pilot readiness is now Stage 11**, **Packaging & commercial base is now Stage 12**. Two stages that were not previously represented as their own gate — production deployment and the controlled pilot — are now explicit **Stage 13** and **Stage 14**. This is a documentation-governance correction only: no completed stage, tag, or historical report has been changed. See the canonical table immediately below for the authoritative sequence.

**Stage 10 (product design system and UX completion) is complete, approved, merged to `main`, and tagged.** Branched from the merged/tagged Stage 9 boundary (`main` `e347b75`, tag `stage-9-complete`). Delivered a token-based design system, a shared component library, and a full visual/interaction-design pass across all seven principal screens — plus direct fixtures for the temporary-provider-outage and uncertain-execution states, a three-way functional/resilience/design Playwright suite split (10/6/26 = 42 tests), and full responsive/accessibility verification — without weakening any safety/privacy/approval semantics. Merged to `main` via PR #5 at `0ecf390389ec523d99c924f62cc2651a32399e06` and tagged `stage-10-complete`. See [ADR 0006](../architecture/adr/0006-stage10-product-design-system.md) and [the design-system doc](../product/design-system.md) for the decisions and reference, and [the Stage 10 completion report](reports/stage-10.md) for the full acceptance-matrix result.

**Stage 11 (evaluation and pilot readiness) is next and has not begun.** Depends on Stage 10. Paid production infrastructure is not required for this stage — it runs against the existing demo/CI environment. Scope (per the original plan, unchanged, only renumbered): closing the deterministic-baseline-only evaluation gap with a blind holdout + adversarial evaluation set (dataset v2, ADR 0002), running the real-provider (`det+anthropic`) evaluation once an Anthropic key is available, a professional review of the draft privacy notice/terms, and assembling pilot-readiness evidence. Stage 12 (packaging and commercial base) depends on Stage 11's evidence being in hand, not merely on Stage 11 having started. **Planning for the human-evaluation track is complete** — see [the Stage 11 plan](stage-11-plan.md), [docs/evaluation/stage-11/](../evaluation/stage-11/) for the full participant-evaluation design and materials, and [the planning completion report](reports/stage-11-planning.md). **Round 1 execution-readiness preparation is also complete**: an evaluation-context decision gate, ethics/privacy enquiry packs, a recruitment-authorisation checklist (currently blocking all recruitment), a participant screener, an operational runbook, a synthetic-scenario manifest with an automated validator, a desk-based protocol rehearsal, and a readiness decision (READY TO REQUEST RECRUITMENT AUTHORISATION — a materials-readiness statement, not an authorisation) — see [stage-11-plan.md](stage-11-plan.md) §12. **The project owner's evaluation-context decision is now recorded**: the current operational mode is OWNER-ONLY INTERNAL VALIDATION (no participant involved); the future participant route, if activated, is INDEPENDENT PRODUCT EVALUATION, potentially supporting a future journal publication — see [evaluation-context-decision.md](../evaluation/stage-11/evaluation-context-decision.md). **Stage 11A owner-only internal-validation Phase 1 (Synthetic Acceptance Validation) is complete: PASS — READY FOR PHASE 2** — 26/26 acceptance-matrix rows verified, 10/10 reset-repeatability cycles clean, full automated suite green (817 backend + 90 frontend tests, 42 E2E journeys, 5 eval modes), zero unresolved P0/P1; the one P2 finding from the original Phase 1 run (F-002, a visual-regression baseline fragility) has since been **closed at root cause** with a deterministic demo-clock control, see [stage-11a-phase-1-plan.md](stage-11a-phase-1-plan.md) and the evidence pack in [docs/evaluation/stage-11/owner-validation/phase-1/](../evaluation/stage-11/owner-validation/phase-1/). **Stage 11A Phase 2 (Controlled Failure and Recovery Validation) is complete: PASS — READY FOR PHASE 3** — all 34 acceptance-matrix rows verified (API/web/worker/scheduler restart, Redis/PostgreSQL outage recovery, provider-timeout/uncertain-write repeatability at the required counts, token-expiry/revoked-consent, cross-user isolation under failure, plus new local backup/restore and rollback rehearsal tooling), full automated suite green (835 backend + 90 frontend tests), zero unresolved P0/P1, see [stage-11a-phase-2-plan.md](stage-11a-phase-2-plan.md) and [docs/evaluation/stage-11/owner-validation/phase-2/](../evaluation/stage-11/owner-validation/phase-2/). **Stage 11A Phase 3 (Security, Privacy and Residual-Data Validation) is complete: PASS — READY FOR PHASE 4 READINESS GATE** — all 44 acceptance-matrix rows verified (owner-scoping across 6 resource families, session/authentication edge cases, an OAuth-credential sentinel lifecycle search, an honest secret-rotation-capability rehearsal, an end-to-end log-privacy sentinel scan, browser-storage inspection, injection resistance, the four privacy operations at required repetition counts, residual-data/tombstone analysis, a new backup-vs-deletion compliance rehearsal, and this project's first dependency-vulnerability and container-hardening scans — which found and fixed two real gaps: a loopback-only local-service-binding fix and several Next.js/postcss/sharp CVEs), full automated suite green (915 backend + 90 frontend tests), zero unresolved P0/P1, see [stage-11a-phase-3-plan.md](stage-11a-phase-3-plan.md) and [docs/evaluation/stage-11/owner-validation/phase-3/](../evaluation/stage-11/owner-validation/phase-3/). Phases B/C (test accounts, soak period) remain planning-only; Phase 3 and any future Phase 4 each require their own separate approval. None of this changes this stage's status: participant recruitment remains not authorised, participant evaluation sessions have not begun, and Stage 12 remains blocked on Stage 11's evidence.

**Stage 9 (privacy, audit UX, resilience) is complete, approved, and merged to `main`.** The Stage 9 Planning Gate (architecture/discovery) is approved and recorded in [ADR 0005](../architecture/adr/0005-stage9-privacy-hardening.md). Delivery was split into five phases: **Delivery Phase 1 — Privacy & Connections Control Centre** (`49f121a`); **Delivery Phase 2 — imported-data deletion, retention enforcement, account deletion** (`fdb4636` on `origin/stage-9-deletion-retention`); **Delivery Phase 3 — audit history** (`a50cf06` on `origin/stage-9-audit-history`); **Delivery Phase 4 — rate limiting** (`481a67b` on `origin/stage-9-rate-limiting`); and **Delivery Phase 5 — outage resilience and privacy-safe telemetry** (`5a2ca516` on `origin/stage-9-resilience-telemetry`) — including all four required outage-simulation Playwright journeys (a test-only fake-Google server plus a dedicated resilience stack, `apps/web/e2e-resilience/`), full provider-metrics coverage of the ingestion read path, and a complete 17-item live manual smoke test — see [the Phase 5 completion report](reports/stage-09-phase-5.md) for the full acceptance-matrix result. All five Delivery Phases were integrated on `stage-9-final-integration` (which also wired the outage-resilience Playwright suite into CI) — see [the Stage 9 closure report](reports/stage-09.md) — and merged to `main` via PR #4 at `e347b75e27399eb353a6d57aa87fe4c2282a803a`, tagged `stage-9-complete`. "Delivery Phase 1" always refers to the Privacy Centre, never the planning gate. Stage 8 remains complete/approved/merged (`main` `c5b60b1`; tag `stage-8-complete` → `a29d6ee`).

**Stage 8 (preferences, memory, schedule) is complete and approved.** All three phases — Phase 1 (explicit preferences), Phase 2 (the scheduled brief, arq+Redis), and Phase 3 (inferred memory) — passed the committed-state closure review as one integrated milestone. Phase 3 delivered one narrow typed memory (`preferred_email_signoff`) with a full transparent lifecycle: observed edited-then-approved drafts → visible candidate with deterministic confidence and inspectable evidence → user confirm/edit/dismiss/delete → confirmed value applied only through the explicit preference registry, previewed and approved like any draft (ADR 0004 D51–D58). The Phase 3 implementation commit is `466de179a7af1fe6410ee4e4f661402bec5b8925`; Stage 8 is merged to `main` at `c5b60b1` and tagged `stage-8-complete` at `a29d6ee`.

Guiding rule: *build one trustworthy end-to-end loop, prove it with tests and evidence, then expand deliberately.* The first end-to-end path is:

```text
Gmail + Calendar → Daily Brief → Proposed Action → Approval → Execution → Audit
```

…delivered **first in demo mode** (Stages 3–6) and only then against real Google APIs (Stage 7).

## Stage summary and gates

| Stage | Goal | Key exit criterion | Depends on |
|---|---|---|---|
| 0 — Discovery & plan (approved) | Precise, buildable MVP plan | Product explainable in one page; first E2E path unambiguous; risks documented | — |
| 1 — Scaffold & foundations (approved) | Reproducible monorepo runs locally | Clean clone → configure → run → test → stop, predictably | 0 |
| 2 — Domain model, auth, isolation (approved) | Secure data foundation | Cross-user isolation proven; migrations stable; token encryption path exists | 1 |
| 3 — Demo mode & synthetic connectors (approved) | First vertical slice, no credentials | Demo user sees coherent normalised information | 2 |
| 4 — Signals & priority engine (approved) | Defensible prioritised signals | Requests/deadlines/follow-ups/conflicts ranked with evidence; injection fixtures safe | 3 |
| 5 — Daily brief (approved) | First genuinely useful outcome | Brief understandable in < 2 min; every actionable statement evidenced | 4 |
| 6 — Proposals & approval inbox (approved) | Safe, editable next steps | Inspect/edit/approve/reject/trace with zero hidden side effects | 5 |
| 7 — Real Google integration (approved) | Proven workflow on real data | Test user connects Google, gets brief, approves draft/event, sees audit | 6 |
| 8 — Preferences, memory, schedule (approved) | Transparent adaptation | Scheduled brief at configured time reflecting visible preferences | 7 |
| 9 — Privacy, audit UX, resilience (approved) | Trust features operational | Users control data; product fails safely in outages | 8 |
| 10 — Product design and UX completion (approved) | Trustworthy, accessible, consistent design | Token-based design system across all principal screens; accessibility automation plus manual checks; direct outage/uncertain-execution UX verification; no safety/behaviour regression | 9 |
| **11 — Evaluation & pilot readiness** (next) | Evidence of usefulness | Acceptance targets met or exceptions documented; pilot pack ready | 10 |
| 12 — Packaging & commercial base (pending) | Demonstrable, pilotable, extensible | Staging deploy passes smoke tests; editions via configuration | 11 |
| 13 — Production deployment & operational readiness (pending) | Safe to operate outside dev/demo | Production infrastructure provisioned and deployed; monitoring, alerting and runbooks proven against a real (non-demo) environment | 12 |
| 14 — Controlled pilot & final release (pending) | Real users, bounded blast radius | A small, monitored pilot cohort onboarded; findings incorporated; general-release criteria met | 13 |

Each stage ends with: all required checks run, implementation inspected (not just exit codes), docs and decision log updated, a completion report in the standard format, then **stop**. Every completion report is archived in [reports/](reports/) as `stage-NN.md` — the project's engineering log.

## Engineering Acceptance Contract

Every delivery phase — regardless of which coding model performs the work — must follow the [Engineering Acceptance Contract](engineering-acceptance-contract.md). It governs how a phase specification becomes a numbered acceptance matrix, the implementation/verification loop the agent runs autonomously, which ordinary engineering gaps must be fixed without a review round-trip, the negative-control and exact-boundary security-proof requirements, and the standard completion-report structure. It applies from Stage 9 Delivery Phase 5 onward; phases delivered before it was adopted (Stages 0–8 and Stage 9 Delivery Phases 1–4) are not retroactively re-audited against it. Every future phase prompt should open with the instruction in the contract's §17.

## Quality gates at every stage

Formatter · linter · type checker · unit tests · relevant integration tests · stage-appropriate security checks · build · documentation check. Tests are never disabled to obtain a green build. From Delivery Phase 5 onward, gates are also governed by the [Engineering Acceptance Contract](engineering-acceptance-contract.md)'s completion conditions (§12).

## External setup the user will eventually need (none blocks demo-mode development)

| Item | Needed by stage | Notes |
|---|---|---|
| Docker Desktop (or compatible) locally | 1 | Postgres via compose |
| Git hosting + CI (GitHub assumed) | 1 | CI config lands with the scaffold |
| Google Cloud project with OAuth consent screen (External, test users), Gmail + Calendar APIs enabled, OAuth client (web) credentials | 7 | Scopes: `gmail.readonly`, `gmail.compose`, `calendar.readonly`, `calendar.events`; test with a sandbox Google account, never a primary mailbox first |
| Anthropic API key | 4 (optional) | Only for LLM-assisted extraction; mock provider covers demo/CI |
| UK/EU hosting provider account | 12 | Chosen in a future ADR (number not yet assigned — ADRs 0001–0006 are already filed for earlier decisions); nothing earlier depends on it |
| Qualified professional review of privacy notice/terms | 11 | Drafts produced in stage 11 |

## Risk register (implementation risks, Stage 0)

| Risk | Impact | Mitigation |
|---|---|---|
| Google OAuth verification friction (restricted Gmail scopes require review for production) | Delays real-user pilot | Develop against test users on an unverified consent screen; scope-minimal design; begin verification paperwork during Stage 7, pilot with ≤100 test users meanwhile |
| LLM extraction quality below targets | Weak briefs | Deterministic detectors are the floor; targets ratified only after baseline (ADR 0002); degraded mode is a feature |
| Two-toolchain drift (TS/Python) | Contract bugs | Generated contracts in `packages/contracts`; contract tests in CI |
| Prompt-injection regressions | Safety failure | Closed action-type enum, policy engine, adversarial fixtures in CI from Stage 4 |
| Scope creep toward editions/connectors | MVP never ships | Out-of-scope list in [../product/mvp-scope.md](../product/mvp-scope.md); extension points recorded, not built |
| Timezone/DST defects (Europe/London) | Wrong deadlines/schedules | UTC storage + tz-aware rendering; DST boundary tests from Stage 3 |
| Solo-developer bus factor / stall | Delivery risk | Stage gates keep the repo always in a documented, runnable state |

## Decision log and documentation index

North Star (long-term vision, permanent principles, guard rails): [../project/project-foundation.md](../project/project-foundation.md). Architecture decisions: [../architecture/adr/0001-architecture.md](../architecture/adr/0001-architecture.md). Assumptions and open decisions: [assumptions-and-decisions.md](assumptions-and-decisions.md). Product scope: [../product/mvp-scope.md](../product/mvp-scope.md). Threats: [../security/threat-model.md](../security/threat-model.md). Engineering delivery process: [engineering-acceptance-contract.md](engineering-acceptance-contract.md). Operational runbooks (Stage 9 Delivery Phase 5): [runbooks/outage-response.md](runbooks/outage-response.md), [runbooks/provider-failure.md](runbooks/provider-failure.md), [runbooks/worker-recovery.md](runbooks/worker-recovery.md), [runbooks/health-readiness.md](runbooks/health-readiness.md). When scope or design questions arise, the Project Foundation document is the single source of truth.
